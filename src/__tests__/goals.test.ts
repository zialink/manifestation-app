// API route test for /api/goals
import request from 'supertest';
// import handler from '../app/api/goals/route';

describe('/api/goals API', () => {
  it('should reject unauthenticated requests', async () => {
    expect(true).toBe(true);
  });
});
